# messages-to-cos — [NAME]

Append-only log of messages this employee sends to Chief of Staff. This file is a symlink (or alias) for `../../inbox/[NAME].md` in OpenHarness.

Format per entry:

```
## YYYY-MM-DD HH:MM
<one-line summary>

<context>

<recommendation if I have one>
```
